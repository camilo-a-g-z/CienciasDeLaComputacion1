#include <iostream>
#include "PILA.h"
using namespace std;

template < class T >  
void reemplazar(Pila<T> x, T valA, T valN){
	Pila<T> aux();
	T datoAux;
	cout<<x.vacia()<<endl;
}

int main(int argc, char** argv) {
	//template < class T >  ;
	Pila<int> acc(5);
	acc.meter(4);
	reemplazar(acc, 5, 5);
	return 0;
}
